---
import Layout from '../layouts/Layout.astro';
---

<Layout title="2667">
  <div class="min-h-screen bg-[#e8e6e3] flex flex-col items-center justify-center px-4 -mt-12">
    
    <!-- Ape Container with Color Reveal Animation -->
    <div class="relative w-64 h-64 md:w-80 md:h-80 mb-12">
      <!-- Outline version (base layer) -->
      <img 
        src="/ape-outline.jpg" 
        alt="2667" 
        class="absolute inset-0 w-full h-full object-contain"
      />
      <!-- Color version (reveals on top) -->
      <img 
        src="/ape-color.jpg" 
        alt="2667" 
        class="absolute inset-0 w-full h-full object-contain color-reveal"
      />
    </div>

    <!-- Terminal Intro -->
    <div class="font-mono text-sm md:text-base text-neutral-800 text-center space-y-2 terminal-intro">
      <div class="terminal-line" style="--delay: 2">
        <span class="text-neutral-500">&gt;</span>
        <span class="typing-text"> initializing 2667_</span>
        <span class="cursor">▋</span>
      </div>
      <div class="terminal-line" style="--delay: 3">
        <span class="text-neutral-500">&gt;</span>
        <span class="typing-text"> status: </span>
        <span class="status-ready text-emerald-600">active</span>
      </div>
      <div class="terminal-line" style="--delay: 4.5">
        <span class="text-neutral-500">&gt;</span>
        <span class="typing-text"> investing in the future of digital culture</span>
      </div>
    </div>

    <!-- Navigation -->
    <nav class="mt-16 flex gap-8 nav-fade">
      <a 
        href="/collection" 
        class="px-6 py-3 border border-neutral-400 text-neutral-700 font-mono text-sm uppercase tracking-wider hover:bg-neutral-800 hover:text-white hover:border-neutral-800 transition-all duration-300"
      >
        Collection
      </a>
    </nav>

  </div>
</Layout>

<style>
  /* Color reveal animation - clip from bottom to top */
  .color-reveal {
    clip-path: inset(100% 0 0 0);
    animation: revealColor 1.5s ease-out forwards;
    animation-delay: 0.5s;
  }

  @keyframes revealColor {
    0% {
      clip-path: inset(100% 0 0 0);
    }
    100% {
      clip-path: inset(0 0 0 0);
    }
  }

  /* Terminal animations */
  .terminal-intro .terminal-line {
    opacity: 0;
    animation: lineAppear 0.1s ease forwards;
    animation-delay: calc(var(--delay) * 1s);
  }

  .terminal-intro .typing-text {
    display: inline;
    overflow: hidden;
    white-space: nowrap;
    max-width: 0;
    animation: typeText 0.8s steps(30) forwards;
    animation-delay: calc(var(--delay) * 1s + 0.1s);
  }

  .terminal-intro .status-ready {
    opacity: 0;
    animation: blinkIn 0.3s ease forwards;
    animation-delay: 4s;
  }

  .terminal-intro .cursor {
    animation: cursorBlink 0.7s step-end infinite;
    animation-delay: calc(var(--delay) * 1s);
    margin-left: 2px;
    color: #10b981;
  }

  .nav-fade {
    opacity: 0;
    animation: fadeIn 0.6s ease forwards;
    animation-delay: 5.5s;
  }

  @keyframes lineAppear {
    to { opacity: 1; }
  }

  @keyframes typeText {
    to { max-width: 500px; }
  }

  @keyframes blinkIn {
    0% { opacity: 0; }
    50% { opacity: 0; }
    100% { opacity: 1; }
  }

  @keyframes cursorBlink {
    0%, 100% { opacity: 1; }
    50% { opacity: 0; }
  }

  @keyframes fadeIn {
    to { opacity: 1; }
  }
</style>
